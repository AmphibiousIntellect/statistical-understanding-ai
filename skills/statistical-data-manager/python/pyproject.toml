[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "suai-statistical-data-manager"
version = "0.1.0"
description = "Python reference implementation of the SUAI Statistical Data Manager"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = ["pandas>=2.0"]
